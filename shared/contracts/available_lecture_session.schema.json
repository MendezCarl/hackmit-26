{
  "$defs": {
    "LectureSession": {
      "description": "One running occurrence of a lecture on the shared lecture clock.",
      "properties": {
        "course_id": {
          "description": "Owning course identifier.",
          "title": "Course Id",
          "type": "string"
        },
        "ended_at": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "UTC ISO 8601 end time.",
          "title": "Ended At"
        },
        "join_code": {
          "default": "",
          "description": "Short human-typeable code students enter to join.",
          "title": "Join Code",
          "type": "string"
        },
        "lecture_id": {
          "description": "Identifier of the underlying lecture record.",
          "title": "Lecture Id",
          "type": "string"
        },
        "mode": {
          "$ref": "#/$defs/SessionMode",
          "description": "How the session is held."
        },
        "owner_id": {
          "description": "Authenticated user who started the session.",
          "title": "Owner Id",
          "type": "string"
        },
        "session_clock_origin": {
          "description": "UTC ISO 8601 origin of the shared lecture clock.",
          "title": "Session Clock Origin",
          "type": "string"
        },
        "session_id": {
          "description": "Unique identifier for this occurrence.",
          "title": "Session Id",
          "type": "string"
        },
        "started_at": {
          "description": "UTC ISO 8601 start time ending in Z.",
          "title": "Started At",
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/SessionStatus",
          "description": "Current lifecycle state."
        },
        "title": {
          "description": "Human-readable lecture-session title.",
          "title": "Title",
          "type": "string"
        },
        "zoom_meeting_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Zoom meeting identifier when authorized.",
          "title": "Zoom Meeting Id"
        }
      },
      "required": [
        "session_id",
        "lecture_id",
        "owner_id",
        "course_id",
        "title",
        "mode",
        "status",
        "started_at",
        "session_clock_origin"
      ],
      "title": "LectureSession",
      "type": "object"
    },
    "SessionMatchSource": {
      "description": "How an active session was matched to the requesting student.",
      "enum": [
        "enrollment",
        "zoom_meeting"
      ],
      "title": "SessionMatchSource",
      "type": "string"
    },
    "SessionMode": {
      "description": "How a lecture session is held.",
      "enum": [
        "zoom",
        "in_person"
      ],
      "title": "SessionMode",
      "type": "string"
    },
    "SessionStatus": {
      "description": "Lifecycle state of a lecture session.",
      "enum": [
        "created",
        "active",
        "ending",
        "ended",
        "failed"
      ],
      "title": "SessionStatus",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "An active session the student can join with one click.",
  "properties": {
    "is_auto_join_enabled": {
      "description": "True when the matching enrollment allows joining without a prompt.",
      "title": "Is Auto Join Enabled",
      "type": "boolean"
    },
    "is_joined": {
      "description": "True when the student is already a registered participant.",
      "title": "Is Joined",
      "type": "boolean"
    },
    "matched_by": {
      "$ref": "#/$defs/SessionMatchSource",
      "description": "Whether the match came from course enrollment or a Zoom meeting id."
    },
    "session": {
      "$ref": "#/$defs/LectureSession",
      "description": "The active lecture session."
    }
  },
  "required": [
    "session",
    "matched_by",
    "is_joined",
    "is_auto_join_enabled"
  ],
  "title": "AvailableLectureSession",
  "type": "object"
}
