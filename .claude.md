# Claude Instructions Compatibility File

The canonical Claude Code instructions are in `CLAUDE.md`, and the repository-wide instructions are in `AGENTS.md`.

Read both files and `docs/README.md` before modifying the repository.
